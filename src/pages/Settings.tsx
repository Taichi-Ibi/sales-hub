/** 特別:設定。モックアップでは中身を持たないプレースホルダ。 */
export function Settings() {
  return (
    <div>
      <h1 className="wiki-h1">特別:設定</h1>
      <p className="text-[13px] text-ink-sub">設定項目はありません（モックアップ）。</p>
    </div>
  );
}
