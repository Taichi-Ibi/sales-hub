/** 設定。モックアップでは中身を持たないプレースホルダ。 */
export function Settings() {
  return (
    <div>
      <h1 className="mb-3 text-xl font-semibold text-ink">設定</h1>
      <p className="rounded-lg border border-line bg-surface px-4 py-5 text-center text-sm text-ink-sub">
        設定項目はありません（モックアップ）
      </p>
    </div>
  );
}
